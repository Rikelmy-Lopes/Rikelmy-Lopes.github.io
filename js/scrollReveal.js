ScrollReveal({
    delay: 250,
    reset: true,
    duration: 1000,
    useDelay: 'always',
    origin: 'top',
    viewFactor: 0.3
})

ScrollReveal().reveal('header');

ScrollReveal().reveal('.inicial');

ScrollReveal().reveal('#about-me-text');

ScrollReveal().reveal('#me');

ScrollReveal().reveal('#image-profile')
